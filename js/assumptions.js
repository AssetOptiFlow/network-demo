// assumptions.js — every modelling assumption, surfaced in the UI.
// Each entry mirrors a labelled ASSUMPTION comment in the source module.

export const ASSUMPTIONS = [
  { area: "Pipeline", text: "STRICT LAYER ORDER — terrain → settlements (+ provisional corridors) → roads → load → transformers → zone subs (grown, sited, capacity-balanced) → THE LADDER (over-cap branches split into SIBLING feeders sharing the exit corridor → stranded sub-worthy blocks spawn a station on their own corridor → 20–200 customer stranded blocks become EXPRESS feeders, the labelled exception → clip only below 20) → PARSIMONY (small stations trial-dissolved with verification and rollback) → PRUNE (feeders under 20 customers removed with their customers and TXs, network rebuilt from the survivors) → subtransmission (visual only). Sub and feeder counts EMERGE from the engineered requirement caps. A validation pass regenerates on a deterministic retry seed when a rule fails, and after 4 attempts keeps the best attempt with reasons reported (never a hard fail). (main.js, membership.js, network.js)" },
  { area: "Geography", text: "100 × 70 km invented map on a 200 m grid (500 × 350 cells, landscape aspect for the browser); sea along one seed-chosen edge; one river traced border-to-ocean by noisy steepest descent (60–210 m wide). Buildable = main landmass, not water, slope < 0.35 m/m. No real data. (terrain.js)" },
  { area: "Settlements", text: "EXPLICIT three-tier hierarchy (targets as shares of sampled customers, so at 50k): ONE large town ≈ 40% (~20,000), each small town 2–4% (1,000–2,000, count set by the slider), and 6–10 rural settlements at 0.4–1% (200–500) — kernel weights are set so realised counts hit the targets, and the rural background gets the remainder. Sites scored on flatness, a coastal↔river blend (Inland slider), river-mouth proximity, and junctions of a provisional least-cost corridor skeleton (anchors ↔ map-edge exits) that later seeds the arterials. σ ∝ √customers (uniform peak density); spacing scales with town size (5 km + 1.6×(σi+σj)); both river banks settled when land allows. (density.js)" },
  { area: "Roads", text: "Inter-town network = corridor skeleton + MST over ALL towns and settlements + 2-nearest-neighbour links (within 45 km), so towns connect in a web with loops, not a bare tree; links touching a rural settlement are collectors, town↔town links are arterials. All A*-routed with slope² cost; sea impassable; first river crossing builds a bridge (28×), later roads reuse it. Urban streets: an organic lattice — spacing scales with population (120–170 m), orientation snapped to the local coast tangent / river axis / terrain contour, every point wobbles, fraying starts near the core, and ~7% of streets drop out (dead ends). Rural roads: spurs off arterials every ~1–1.6 km (many fork), then nearby dead ends are linked into a sparse web of back roads; rural LOAD follows roads, not vice versa. (roads.js)" },
  { area: "Load", text: "Density = per-town kernels — Gaussian core + exponential shoulder, so density eases through a peri-urban fringe instead of cliff-edging at the town boundary (mass-compensated, area ∝ population) — + rural background decaying with ROAD distance, + an off-road shoulder (farms up to ~6 km past the road end), × fBm noise. 50 000 customers (fixed) sampled from this field on buildable land, then snapped to the road graph; those landing on feeders under 20 customers are pruned, so the served count emerges slightly lower. (density.js, customers.js, main.js)" },
  { area: "Electrical", text: "Distribution transformers: the TX sits AT a seed customer (fixed pole site) and gathers neighbours within 500 m, up to 100 customers — the ≤ 500 m rule holds exactly by construction. Rural TXs max out on distance, urban on count. A TX more than 400 m from any road connects by a LINE EASEMENT — a straight cross-country span to the nearest road node or a nearer easement node (spans daisy-chain up remote valleys) — so rural feeders may leave the road corridor. Easements never span sea or lakes; river spans are allowed (towers, not bridges). Crew travel is charged along the line route — an approximation. LV below the TX not modelled. (network.js, membership.js)" },
  { area: "Electrical", text: "Zone subs: TXs cluster by road-Dijkstra growth (≤ 50 km or ≤ 5,000 customers); each initial sub is sited at its cluster's LOAD-WEIGHTED CENTROID nudged to an arterial/collector node; LADDER-spawned subs sit AT a load-weighted median node ON their own corridor (so the re-bid is guaranteed to hand them their neighbourhood). Every TX joins its road-nearest sub via an ADDITIVELY WEIGHTED graph Voronoi: over-cap subs bid their boundary out, starved subs bid back in. Station budget 60; stations need ≥ 200 customers to exist — smaller ones are TRIAL-DISSOLVED by the parsimony pass (remove, re-bid, verify receivers stay ≤ 90% of caps and express/clip counts don't grow, else roll back — replaces the old unverified distance merge that manufactured super feeders). (network.js)" },
  { area: "Electrical", text: "Feeders: each busbar branch of a sub's shortest-path tree is CUT into connected clusters of ≤ 1,000 customers and ≤ 200 km conductor. Clusters near the busbar are SIBLING feeders — own breaker position (≤ 12 per station), unloaded exit lead ≤ 2 km running in parallel along the shared corridor, exactly how real urban subs run 8–12 feeders. The released boundary span between siblings becomes a normally-open SIBLING TIE. A stranded cluster (lead > 2 km) of ≥ 200 customers earns its own station; one of 20–200 customers becomes an EXPRESS feeder — a long unloaded lead to a distant block, permitted as a labelled exception, never the rule (LOUD when a sub-worthy block was stranded only by the exhausted station budget). A feeder still under 20 customers after folding is PRUNED with its transformers and customers (uneconomic to reticulate). Residues are CHECKED and reported, never absorbed. Sibling/express circuits on shared structures fault INDEPENDENTLY in the model — common-mode events (car vs pole line) are not modelled. (network.js, main.js)" },
  { area: "Electrical", text: "Line sections in cells above the density threshold are UNDERGROUND cable (drawn dashed), the rest overhead. λ defaults: 0.08 OH / 0.02 UG faults/km/yr, both adjustable; repair is a flat 120 min for both — a simplification that flatters cable repairs. (network.js, reliability.js)" },
  { area: "Subtransmission", text: "GXP on a flat map-edge cell near the load centroid (prefers road corridors). Lines GXP → subs are least-cost A* (slope penalised, ocean blocked, river crossings 6×, road corridors rewarded); adjacent subs share trunk corridors before branching (0.35× on used cells) and one inter-sub tie is routed to AVOID the trunks. VISUAL ONLY — a check asserts SAIDI and network structure are unchanged by (re)building it. (subtx.js)" },
  { area: "Reliability", text: "Fault rates uniform within each line type — no weather, vegetation or asset-age variation. Debug mode can double λ on one branch. Unrestored outage = crew travel from the zone sub at 50 km/h along roads + flat 120 min repair. LATERAL FUSES are STANDARD CONSTRUCTION, part of the device-free baseline (no utility operates fuseless): every section with ≤ 100 downstream customers is fuse-protected — a fault there drops only its own subtree for travel + repair, the feeder rides through. Deepest fuse wins (ideal coordination); fuse replacement folded into the flat repair; devices on fused laterals gain nothing, so greedy spends its budget on trunks. A fault on a feeder's unloaded exit LEAD interrupts that whole feeder. (reliability.js)" },
  { area: "Reliability", text: "Sectionalising: open deepest upstream switch, reclose the tripped device; everyone else in the tripped zone restored after a flat 45 min. Reclosers are ideal protection: upstream customers see NO sustained interruption; momentaries (SAIFI/MAIFI) not modelled; reclosers double as isolators for backfeed. BACKFEED: adjacent feeders share one normally-open tie (shortest unused road/easement edge ≤ 2 km, built in network.js); for a fault, every maximal device-bounded subtree below it that reaches a tie is re-energised from the neighbouring feeder at the same flat 45 min — tie capacity UNLIMITED and the neighbour assumed healthy (labelled simplifications). Greedy candidate gains are EXACT per-feeder re-evaluations, so placement stays provably monotone. Baseline is device-free. (reliability.js)" },
  { area: "Reliability", text: "Devices-for-improvement table: fewest devices of each kind, placed greedily from a DEVICE-FREE network, to cut SAIDI by ≥ 10/25/50%; caps 120 SW / 60 RC match the UI inputs. (main.js)" },
  { area: "Visual", text: "SAIDI heat layer shades each customer cell by the serving feeder's customer-minutes, normalised to the current worst feeder (dark = worst) — a view of the league table, not an input. (render.js)" },
  { area: "General", text: "Plausible, not optimal — greedy/heuristic everywhere, matching the untidy way real networks accrete. Seeded and deterministic throughout, including validation retries." },
];
